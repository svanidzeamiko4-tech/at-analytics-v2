# Amiko Analytics V3 — application dependencies (stdlib excluded).
# Install: pip install -r requirements.txt
# OCR: install Tesseract on the OS and ensure it is on PATH (pytesseract wraps the binary).

streamlit>=1.57,<2
pdfplumber>=0.11,<1
PyMuPDF>=1.24,<2
pytesseract>=0.3.10,<1
pandas>=2.2,<4
plotly>=5.24,<7
pillow>=10.4,<12
python-dotenv>=1.0,<2
anthropic>=0.49,<1
# Enterprise Phase 0–1 (PostgreSQL migration — optional until USE_POSTGRES=true)
sqlalchemy>=2.0,<3
psycopg2-binary>=2.9,<3
psycopg[binary]>=3.1,<4
alembic>=1.13,<2
pydantic-settings>=2.0,<3
loguru>=0.7,<1
# Phase 2 — RS.GE background worker (separate process, not Streamlit)
apscheduler>=3.10,<4
# Phase 4 — auth security (bcrypt password hashes)
bcrypt>=4.1,<5